name: Daily Report
on:
  schedule:
    - cron: '30 0 * * 1-5'
  workflow_dispatch:
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: pip install akshare pandas --quiet 2>/dev/null || true
      - name: Run
        run: python3 daily_report.py
        env:
          WX_PUSH_KEY: ${{ secrets.WX_PUSH_KEY }}